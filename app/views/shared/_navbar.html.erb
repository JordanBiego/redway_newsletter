<nav class="navbar navbar-expand-md bg-dark" data-bs-theme="dark">
  <div class="container-fluid custom-container">
    <a class="navbar-brand">
      <%= image_tag("logo.png", width: "8%") %>
    </a>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle text-light" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <span class="flag-icon flag-icon-br"></span> <%= t('language') %>
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown" style="margin-top: 8px;">
            <a class="dropdown-item" href="<%= url_for(locale: :en) %>"> <%= image_tag("US.png") %> English</a>
            <a class="dropdown-item" href="<%= url_for(locale: :pt) %>"> <%= image_tag("BR.png") %> Português</a>
          </div>
        </li>
        <a>
        <% if user_signed_in? %>
          <%= link_to 'Logout', destroy_user_session_path, class:'btn btn-primary btn-lg btn-block' %>
        <% else%>
          <%= link_to "Admin", new_user_session_path, class:'btn btn-primary btn-lg btn-block'%>
        <%end%>
        </a>
      </ul>
    </div>
  </div>
</nav>
